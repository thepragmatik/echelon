# Echelon Workers
Ephemeral worker scripts for Build and Review Managers.
- implementer.sh — clones repo, implements, compiles, checkpoint-commits, opens PR
- reviewer.sh — shallow-clones PR branch, reviews code, posts structured verdict
