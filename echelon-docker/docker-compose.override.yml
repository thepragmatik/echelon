services:
  builder:
    profiles: ["staging"]
    environment:
      - ECHELON_ENV=staging
      - LOG_LEVEL=DEBUG
  reviewer:
    profiles: ["staging"]
    environment:
      - ECHELON_ENV=staging
      - LOG_LEVEL=DEBUG
  privacy-router:
    profiles: ["staging"]
  redis-primary:
    ports: ["16379:6379"]
